# Benchmarks

Rough throughput on an NVIDIA RTX 2070 (N = 1024), single run:

| Kernel        | Time (ms) |
|---------------|-----------|
| matmul_naive  | 12.4      |
| matmul_tiled  | 4.1       |

Numbers are indicative only — measure on your own hardware.
